import { IsString, IsNotEmpty , Matches } from 'class-validator';
export class CreatePostDto {
  // @IsString()
  // @IsNotEmpty()
  //  @Matches(/\S/, { message: 'title should not be just spaces' })
  // title: string;

  // @IsString()
  // @IsNotEmpty()
  // @Matches(/\S/, { message: 'content should not be just spaces' })
  // content: string;

// @IsString()
// @IsNotEmpty()
// @Matches(/\S/, { message: 'content should not be just spaces' })
// name: string;

//   @IsString()
//   @IsNotEmpty()
//   @Matches(/\S/, { message: 'email should not be just spaces' })
//   email: string;

//   @IsString()
//   @IsNotEmpty()
//   @Matches(/\S/, { message: 'password should not be just spaces' })
//   password: string;

} 